# 6. Buatlah function untuk mengimplementasikan algoritma pengurutan daftar(array)
sample_array = ["1", "4", "0", "6", "9"]


def sort_list(array_samp):
    array_samp.sort()
    return array_samp


print(sort_list(sample_array))
